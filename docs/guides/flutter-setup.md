# Flutter Setup Notes

This repo includes source-complete Flutter apps, but Flutter SDK was not installed in the current execution environment.

## Customer app
- Entry: `apps/customer_app/lib/main.dart`
- Focus: landmark-first booking, ride selection, active trip, support references

## Driver app
- Entry: `apps/driver_app/lib/main.dart`
- Focus: online/offline state, incoming requests, trip controls, earnings snapshot

## Recommended next step
- Install Flutter locally
- Run `flutter pub get` in both app folders
- Add API/http and socket clients to replace the demo services
